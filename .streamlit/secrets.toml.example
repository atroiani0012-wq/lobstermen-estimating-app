# Copy to .streamlit/secrets.toml and fill in your key.
# On Streamlit Community Cloud, set these in the app's "Secrets" panel instead.
ANTHROPIC_API_KEY = "sk-ant-api03-REPLACE_ME"
ANTHROPIC_MODEL = "claude-sonnet-4-6"

# Optional: Google Drive integration (see SETUP.md). Only needed if you plan
# to use the "Google Drive folder" input mode for large bid packages.
# Paste the full contents of the service account JSON key here under this
# header. Use triple-quoted strings for private_key so newlines are preserved.
# [google_service_account]
# type = "service_account"
# project_id = "uma-estimating"
# private_key_id = "…"
# private_key = """-----BEGIN PRIVATE KEY-----
# …
# -----END PRIVATE KEY-----
# """
# client_email = "uma-estimating-reader@uma-estimating.iam.gserviceaccount.com"
# client_id = "…"
# auth_uri = "https://accounts.google.com/o/oauth2/auth"
# token_uri = "https://oauth2.googleapis.com/token"
# auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
# client_x509_cert_url = "…"
# universe_domain = "googleapis.com"
